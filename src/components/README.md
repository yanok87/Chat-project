# components

Reusable UI:

- **ui/** – atoms (Button, Input, etc.) and small primitives
- **chat/** – visitor widget, message list, bubbles, input
- **agent/** – inbox list, thread view, reply input

To be added in later steps.
